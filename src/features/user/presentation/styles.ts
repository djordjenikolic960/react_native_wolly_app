import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  header: {
    color: '#AEB6CE',
    marginBottom: 14,
    fontWeight: 'bold',
    marginTop: 14,
  },

  title: {
    fontWeight: 'bold',
    fontSize: 16,
  },

  subtitle: {
    color: '#AEB6CE',
    fontSize: 12,
    marginBottom: 14,
  },
});
